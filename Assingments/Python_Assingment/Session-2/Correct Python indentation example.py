# Intentionally bad indentation example (commented out so the script can still run):
# if True:
# 	print("This line uses a tab for indentation.")
#     print("This line uses spaces, which causes an indentation error in Python.")

# Correct version: use only spaces for indentation


print("Corrected version:")
if True:
    print("This script uses only spaces for indentation.")
    print("It runs without errors.")

