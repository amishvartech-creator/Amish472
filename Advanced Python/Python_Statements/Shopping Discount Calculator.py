# Write a Python program to apply discount:

# def apply_discount(total_amount, discount_rate):
#     discount = total_amount * discount_rate
#     final_amount = total_amount - discount
#     return final_amount

# # Example usage:

# total_amount = int(input("Enter the total amount: "))
# discount_rate = int(input("Enter the discount rate (0-1): "))

# final_amount = apply_discount(total_amount, discount_rate)
# print(f"Final amount after discount: ${final_amount:.2f}")

